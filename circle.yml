machine:
  timezone:
    Asia/Tokyo
  php:
    version: 5.5.11
dependencies:
  pre:
    - pip install awscli
    - gem install bundler
  override:
    - php composer.phar install
test:
  override:
    - echo '1'